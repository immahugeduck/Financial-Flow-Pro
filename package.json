{
  "name": "financial-flow-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "test": "cd frontend && npm test"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
